# input/

Your reference art goes here. Point the `ART` / `TEAL` / `RENDER` paths in
`src/vvg_config.py` at your files, then follow "Using your own reference
art" in the main README.

Images in this folder are gitignored on purpose - the art stays local.
